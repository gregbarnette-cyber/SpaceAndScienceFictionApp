# Test Suite — Per-File Descriptions

Read-on-demand reference (deliberately NOT auto-loaded from CLAUDE.md — large).
Read this before adding or modifying tests. The DB-isolation patterns, the
offline/network-gated split, and the `SPACE_APP_DB` convention are summarized in
CLAUDE.md; the full per-file catalog lives here.

## Tests

Tests live in `tests/`. The bulk are **offline** and need no network or Qt:

- `test_equations.py`, `test_calculators.py`, `test_regions.py` — pure math/physics core (HZ + Kopparapu Seff, brachistochrone profiles, velocity/coordinate conversions, planetary & rotating-habitat equations, the spectral-type ceiling rule).
- `test_worldbuilding.py` — the Phase H worldbuilding calculators (Roche limit, tidal-locking timescale, Hill sphere, binary orbit stability, atmosphere retention) in `core/equations.py`; anchored to reference values and locks the two formula corrections (rigid Roche coeff 1.26, binary P-type `+4.12μ`). **Phase T1a** adds the additive Hill-sphere **Domingos 2006 exomoon keys** (`stable_fraction`/`stable_moon_limit_km/au`, prograde leading 0.4895 / retrograde 0.9309, inclination deg→rad) plus a regression guard that the pre-existing Hill-sphere keys are unchanged when the new `moon_inclination_deg`/`prograde` args are omitted.
- `test_solvent_zones.py` — the Phase P (Snow Lines & Alternative-Solvent HZs) core + query.py + viz contracts (offline). `compute_solvent_zone` (P4, M1 surface model): the legacy alt-HZ divisor anchors at A=0.3 (water/ammonia/methane), the corrected `(1−A)^0.25` albedo exponent, custom ranges, the CO₂ pressure-conditional flag, the corrected hydrogen band, the validation matrix, and the `solvent-zone` subcommand (subprocess happy-path/parity + the exit-code matrix — curated `{"error"}` exit-1, mutex/missing-arg exit-2). `compute_ice_lines` (P5, M2 equilibrium): the 278.5 K `T_ref`, the canonical 170 K / 2.68 AU water snow line, the CO₂/NH₃/N₂/CO fronts (N₂/CO flagged `disk_line`), no dual/formation line, + the `ice-lines` subcommand. The P1 value re-anchors (hydrogen ÷0.0000247/÷0.0000053, snowLine ÷0.139, `planetaryTemperature` `(1−A)^0.25`), the P2/P3 additive region keys (derivation parity + sound-bands regression guard), and the viz preps `prepare_solvent_ranges` (V5), `prepare_ice_line_diagram` (V4), `prepare_orbit_overlays` (V6/V7, all solvent zones default-off). The V1 10-band alt-HZ / V2 system-regions relabels are exercised via `core.viz`. (The Phase O `test_viz_phase_o.py` additivity guard covers `make_orbits_canvas` with no `snow_au`/`solvent_bands`.)
- `test_db_backups.py` — the opt-50 backup pruner (`core.db.prune_star_systems_backups`).
- `test_search.py` — the Phase G search/filter core: `core.shared.spectral_where` / `spectral_adql` (pure clause builders), the DB-backed `search_star_systems` / `search_hwc` (temp DB, auto-seed disabled, no network — incl. the `ss`-aliased query), and `search_exoplanets` with the NASA TAP fetch mocked. (The Phase L4 `search_star_systems` `fe_h` JOIN is exercised separately in `test_hypatia_cache.py`.)
- `test_shared.py` — offline unit coverage for `core/shared.py` helpers (seeded IMPROVEMENT_PLAN Phase 6, expanded Phase 7; pure offline, no Qt/network). The P6.1 `_with_retries` (success/exhaustion + backoff) and its **`Retry-After`** honoring (`_retry_after_seconds`: integer-seconds, capped at 60, HTTP-date form falls back to backoff), plus (P7.1) `_parse_designations` / `_parse_designations_from_ids` (incl. the `Gaia DR3`→`EDR3` slot rule, DR2 exclusion, first-prefix-wins, the `keys=` subset), `_parse_spectral_class` (`sdG5` prefix-skip, DA/DZ white-dwarf rejection, fractional subtypes, sentinels), `_lookup_spectral_type` (ceiling rule G1→G2 / cross-letter fallthrough F9→G0 / clamp M9→M8 / white-dwarf→None, reading the real `propertiesOfMainSequenceStars.csv` under the `save/restore_main_sequence_cache` guard), and the `_network_error_msg` classification matrix (requests Timeout/ConnectionError, urllib URLError timeout-vs-generic, string fallbacks, raw passthrough).
- `test_databases.py` — offline unit coverage for `core/databases.py` (seeded Phase 6, expanded Phase 7; no network/Qt). The P6.2 `_adql_quote` (single-quote doubling + control-char strip + injection neutralization) and P6.3 `_validate_csv_headers` (+ `import_hwc_csv` rejecting a malformed header before any DDL), plus (P7.2) the archive readers with the network mocked — `compute_exoplanet_archive` (happy / no-designation / error-passthrough / no-planets / TAP-timeout-classified / HWO-non-fatal / `_adql_quote` applied in the built WHERE), `compute_planetary_systems_composite`, `compute_hwo_exep` (GJ-priority) via a mocked `_query_tap`; the DB-backed `compute_mission_exocat` (temp CSV through the real `import_mission_exocat_csv` path) and `compute_hwc` (the real `hwc.csv` imported into a temp DB — HD/HIP match, HD 28109 multi-planet SMA sort); and `_query_tap` retry-exhaustion (mocked `requests.get` → Timeout) surfacing → caller classification. Same temp-DB pattern as `test_search.py`.
- `test_gcns.py` — GCNS ingest/query path with the GAVO TAP fetch mocked.
- `test_oec.py` — offline Open Exoplanet Catalogue coverage (`_norm_oec_name`, `_oec_node` generic capture, `compute_oec`/`compute_oec_planet`, the shared display formatters). Monkeypatches `core.databases._oec_get_root` to parse an **inline fixture XML** (the index is still built off it) covering every real topology — single star, S-type binary, P-type circumbinary, hierarchical depth-2, rogue, zero-planet — plus the mockup-surfaced hazards: multi-`<list>` on a binary planet, arcsec+AU `separation`, a bound-only (`upperlimit`) value, `mass type="msini"`, a `<satellite>` moon, an unnamed binary, and alias/planet-name resolution. **Also the Phase-3 System Architecture layout** (`core.viz.prepare_oec_architecture`): `ArchitectureMathTests` unit-tests the pure barycenter/Kepler math on hand-built node dicts (mass-weighted offset, Kepler-from-period rung, missing-mass equal split, no-separation schematic fallback, AU-preferred-over-arcsec, arcsec→AU projection); `ArchitectureTopologyTests` runs it off the shared fixture (single-star-at-barycenter / no rings, depth-2 hierarchy places all stars + emits recenter handles, rogue → error, focus-recenter on a subsystem/star, white-dwarf spectral colour, and the Phase-3b `centers` regression: star-hosts emit none, the fixture circumbinary emits one). **Phase-3b interactive coverage:** `ArchitectureCentersTests` unit-tests the circumbinary (P-type) `centers` payload (emitted at the binary barycenter, planet absent from component stars, survives a focus on the binary, a nested pair keyed to its own offset barycenter). `ArchitectureCanvasTests` + `OecPanelRecenterTests` are **Qt/matplotlib-gated** headless smoke tests (offscreen `QApplication`): the canvas builds for every topology in both interactive/static modes, a simulated `pick_event` on a star / ◆ handle fires `on_select` with the right node, the circumbinary canvas draws its extra ring patch, and the real `OecPanel` recenter wiring (`_on_arch_select`/`_on_arch_reset`/`_rebuild_after_focus`, driven via a `_FakeWindow`) updates the map focus + host, keeps the Architecture map at viz tab 0, and the breadcrumb reflects the focus. **Click-a-planet parity:** planet dicts carry their `node`; a simulated pick on a planet dot fires `on_planet_click` (not a recenter) with the planet + host label (a star name, or the binary label for a circumbinary planet), planets aren't pickable without the callback, `_show_oec_planet_dialog` renders the fields (M·sin i, status), and a click on the live panel canvas opens a `QDialog` parented to the panel. Offline (no network); Qt/mpl skip cleanly when absent. **Phase-4 structural readers:** `OecSearchTests` (fixture-backed `compute_oec_search` — system + planet filters, `--circumbinary`, `--spectral-type` prefix, Jupiter-unit mass ranges, conjunctive planet filters, inverted-range/`--limit` validation, cap/`capped`) and `OecCensusTests` (`compute_oec_census`/`compute_oec_status` counts + distributions + `planet_attachment` off the fixture).
- `test_oec_live.py` — **live network** (gated by `tests/_netcheck.reachable("github.com")`): downloads the real `systems.xml.gz` once (cached under `data/oec/`) and asserts `compute_oec` resolves Alpha Centauri (≥3 stars in one tree) + an HD alias, a planetless star is absent, and Kepler-16 b is `attached_to: "binary"`. **Phase 4:** `compute_oec_census` over the real catalogue has planets attached to star/binary/system (all > 0) with circumbinary/rogue systems present, `oec-search --circumbinary` finds Kepler-16, and `oec-status` reports counts ≥ `_OEC_MIN_SYSTEMS`.
- `test_simbad_gcns_enrichment.py` — the Phase M5 `compute_simbad_lookup` `"gcns"` cross-reference (non-fatal/silent when no Gaia id / not in GCNS / table empty); SIMBAD mocked, seeded temp `gcns_stars`.
- `test_hypatia_elements.py`, `test_parse_composition.py`, `test_prepare_abundance_profile.py`, `test_gui_hypatia.py` — Hypatia element table, composition parsing, and abundance-profile prep.
- `test_route_planning.py` — the Phase I route-planning core (`compute_multi_stop_journey`, `compute_nearest_neighbor_chain`, `compute_trade_route_mst`) + `core.viz.prepare_route_map`; offline via a tmp seeded `star_systems` (the `test_db_backups.py` pattern) with `Sol`-origin resolution and one mocked-SIMBAD-fallback branch; hand-checkable fixture geometry.
- `test_route_planning_opts.py` — the Phase I-OPTS route planners (`compute_optimal_tour` NN+2-opt, `compute_jump_route` Dijkstra/BFS, `compute_jump_network` BFS tiers, `compute_farthest_first_chain`) + their `core.viz.prepare_route_map` branches; same offline seeded-`star_systems` pattern (Sol origin + one mocked-SIMBAD branch), with a `_radec_for_xyz` helper to place stars at arbitrary 3D points (the jumps-vs-distance fixture).
- `test_honorverse_expansion.py` — the Phase K Honorverse calculators (`compute_hyper_translation_time`, `compute_impeller_wedge` in `core/science.py`; `compute_missile_intercept` in `core/calculators.py`) + the K0 refactor parity (opt-15/16 output byte-identical after the band/mass tables were hoisted to module constants; the 24-band expanded table locked by value incl. the Iota=6000 / merchant-drift correction). Pure math, offline (no DB/network).
- `test_query_expanded.py` — the `query.py` expansion subcommand contracts: Search & Filter (`search-star-systems`, `search-hwc`, `search-exoplanets`), reference data (`main-sequence`, `solar-system`, `sol-regions`), and the planetary/rotating-habitat equations (`orbit-distance`, `moon-orbital-distance`, `gravity-acceleration`/`-distance`/`-rpm`, `travel-time-custom-thrust`). Class-level throwaway `SPACE_APP_DB` (auto-seeded reference tables + seeded `star_systems` + imported `hwc.csv`); offline happy-path + argparse exit-2 matrix; the two network entries (`search-exoplanets` NASA TAP, `travel-time-custom-thrust` JPL Horizons) are reachability-gated. Documents that the equation wrappers are **non-self-validating** (Phase-N-style: raw-exception exit-1 or nonsense exit-0).
- `test_query_route_opts.py` — the `query.py` Route Planning subcommand contracts for **all seven** planners (`optimal-tour`, `jump-route`, `jump-network` + the backfilled `multi-stop`, `nearest-neighbor`, `farthest-first`, `trade-route`): offline subprocess happy-path + the exit-code matrix (curated `{"error"}` exit-1 for out-of-range since these wrap self-validating functions; `jump-route` unreachable & `stopped_early` chains are exit-0; argparse exit-2 for bad `--optimize`/non-int `--hops`/missing/both-velocity). Seeds a throwaway `SPACE_APP_DB`.
- `test_query_phase_n.py` — the Phase N `query.py` subcommand contracts (`habitable-zone-sma`, `star-luminosity`, `brachistochrone-au`/`-lm`, `travel-time-solar`): offline subprocess happy-path + parity + the exit-code matrix (raw-exception exit-1 for the non-validating wrappers; `star-luminosity`'s no-error-path; argparse exit-2), plus the mocked `travel-time-solar` dispatch wiring (arg mapping; `progress_callback` never passed). The live Horizons round-trip is gated on JPL reachability (skips offline).
- `test_query_phase_t.py` — the **Phase T** `query.py` research-tooling extensions for the sibling `scifiWorldBuilding-Claude` repo (offline; subprocess harness mirroring `test_query_phase_n.py`, all **self-validating** Phase-H/P — curated `{"error"}` exit 1, argparse exit 2). **T1a** (5 items): `trojan-stability` (wraps R2 `feasibility.gascheau_coorbital_stable`; μ-flip at ≈0.0385), `lorentz-factor` (γ at β=0/0.6/0.999), `circumbinary-hz` (lum-weighted `eff_teff` + `out_of_range_teff` flag-not-clamp), the additive `hill-sphere` Domingos exomoon keys, and the `gcns-within-sol --wd-prob-min/max` white-dwarf filter (seeded throwaway `gcns_stars`). **T1b** (8): the detectability group `rv-semi-amplitude`/`transit-signal`/`astrometric-signal`/`direct-imaging` (anchored Earth→Sun 83.9 ppm / 0.0895 m/s / 5.4e-10, Jupiter→Sun@10pc 496 µas; period↔SMA parity), `tidal-heating` (order-of-mag, leading 21/2; surface-flux consistency), `kozai-lidov` (order-of-mag, leading 8/15π; period↔SMA parity), `relativistic-brachistochrone` (low-speed → the Newtonian flip-burn `2√(D/a)` cross-check vs `compute_travel_time_system_au`; proper < coordinate near c), and the `circumbinary-hz --star1/--star2` SIMBAD-resolve mode (in-process, mocked SIMBAD/regions). **T1c** (2): `solar-analogs` (twin/analog box over `hypatia_cache` + opt-in `--gcns-distance` cross-match join + the mandatory `population` caveat field) and `substellar` (L/T/Y over `gcns_stars` by spectral-type prefix + the mandatory `completeness_note` field), both over seeded throwaway DBs. The three pinned coefficients (Domingos retrograde 0.9309/1.0764/0.9812, tidal 21/2, KL 8/15π) were verified against the cited papers at build.
- `test_dust_query.py`, `test_dust_routing.py` — the **Phase T2** dust / ISM path (`core/dust.py` + `core/dust_routing.py`), gated on `dustmaps` importability via `tests/_dustcheck.py` (mirrors the `*_live.py` network gate, so a checkout without the optional `dust` extra skips them). The bulk is **non-gated** because the maps are **mocked** (no 5.6 GB download needed): `test_dust_query.py` covers Part A — the dependency-isolation guard (`core.calculators` must not import `dustmaps`, subprocess-checked), the extra-missing error path, the engine math/seam/coverage via a mocked `_query_native` (Leike `A_V=1.0857·1.202·τ_G` / Edenhofer `A_V=2.8·E`, auto-seam at ~69 pc, out-of-coverage null-not-clamped), the validation/exit-code matrix, the map-not-fetched path, a **fetch-gated real-data anchor**, and the GUI `FetchDustMapPanel` smoke (construction, the disabled-without-extra gate, the check/error render paths, and the copyable Zenodo manual-download command box). **Non-gated status surface:** the pure-pathlib `core.dust.get_dust_map_status()` (shape/keys, missing-file → null size, present-file → size, all without importing `dustmaps`) and a `DbStatusPanel` smoke asserting the opt-57 panel appends the two cached map **files** (relabeled `Table / File` / `Rows / Size` columns, Present/Missing) beneath the DB tables. `test_dust_routing.py` covers Part B — the routing logic (least-dust detour with the distance-optimal `extra_ly`/`saved_av` comparison, uniform-dust-coincides-with-distance, MST/NN selection, optimal-tour shape) via a mocked `_seg`, an integration-wiring check (real `_seg`→`integrate_segment_av`, mocked map, constant density), the preflight/exit-code matrix, and the `--weight distance` delegation guard. The byte-identical `_grid_search` extraction from `compute_jump_route` is guarded by the existing `test_route_planning_opts.py` / `test_query_route_opts.py`. **Phase AD (C11)** adds `jump-route --weight blend` (`compute_jump_route_blend`, jump-route only) with `--alpha`/`--beta`: each edge costs `α·distance_ly + β·A_V` over the same `_grid_search` Dijkstra. `test_dust_routing.py` (mocked `_seg`, seeded DB): `--beta 0` reproduces the `--weight distance` path (== `calc.compute_jump_route`), `--alpha 0` reproduces the `--weight dust` path (== `compute_jump_route_dust`), raising `--beta` flips the route between the two, `total_blend_cost = α·total_ly + β·total_av`, and the validation matrix (negative α/β, both-zero, bad max-jump). `test_query_route_opts.py` adds the two offline handler/argparse guards (α/β without `--weight blend` → exit 1; bad `--weight` choice → exit 2). Reachability stays geometric; the other four planners keep `{distance,dust}`.
- `test_cooling_hz.py`, `test_query_cooling_hz.py` — the **Phase U** cooling-primary HZ-residence calculator (`core/cooling.py` + `core/cooling_tables.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo, Phase-N/T lineage (no GUI, no network, no DB). `test_cooling_hz.py` (in-process): the bundled-table integrity (every Bédard-2020 WD + ATMO-2020 BD row passes the closure `L=(R/R☉)²(Teff/T☉)⁴`), (mass,age) interpolation reproducing grid nodes + the off-grid raise, the three modes against the real **0.6 M☉ WD** track — **snapshot** (5000 K → L≈8.6e-5 / cons HZ 0.0092–0.0167 AU; teff↔age parity; hot-young `out_of_range_teff` flag-not-clamp), **residence** (a=0.01 AU → ~7.4 Gyr optimistic reproducing Fossati 2012 / ~4.5 cons; the crossing direction; far/near orbits never habitable — the asymmetric hot-only Kopparapu gate), **CHZ** (0.0065–0.0198 AU reproducing Agol 2011, reproduced across 0.4–0.9 M☉; the fluid-Roche cool-WD collision per Pkt 7 R2; threshold narrows the band) — the **BD** track (residence rises with mass, ~0.3 Gyr at 13.6 M_Jup → multi-Gyr only for >~52 M_Jup per Bolmont 2011/2017, the cold-host portion flagged `exit_out_of_range`; the M_Jup↔M☉ conversion), and the validation matrix + determinism. `test_query_cooling_hz.py` is the offline `query.py cooling-hz` subprocess contract (mirrors `test_query_phase_t.py`): the three-mode happy-path JSON + core-parity, the BD happy-path, and the self-validating exit-code matrix (off-grid mass / `--sma-au 0` → curated `{"error"}` exit 1; missing/bad `--track`, two mode args, two mass args, non-numeric → argparse exit 2). **Phase AD (A0)** adds the WD-only **²²Ne distillation cooling pause** `cooling-hz --cooling-delay-gyr Δ` (default 0 = off) `--distillation-teff-k` (default **5500 K**, the 0.6 M☉ DA onset of Vanderburg+2025 arXiv:2501.06613 §2): a wall-clock→track-age warp (`_warp_age`) freezes (Teff,L,R) at the distillation epoch for Δ Gyr, then resumes (ceiling extends +Δ). `test_cooling_hz.py` A0 class: the headline **Δt=0 byte-identical regression pin** across all three modes (no `pause_*` keys leak), `pause_teff_k≈5500` + `pause_hz_inner/outer_au` + `effective_age_max_gyr`, residence at a frozen-band orbit rises by ~Δ, **peak residence 6.3→16.3 Gyr at Δ=10 (Vanderburg Table 1: 6.67→15.56)**, the CHZ outer edge moving outward at long-residence thresholds (thr 6 → outward; thr 8 → standard yields no CHZ while the pause creates one — the documented finding that for 0.6 M☉ the ≥3 Gyr CHZ is already wide so the effect shows at ≥5 Gyr), snapshot-through-pause, determinism, and the validation matrix (neg Δ / distil-Teff ≤0 or off-track / **Δ>0 on `--track bd` rejected** since distillation is a WD mechanism). `test_query_cooling_hz.py` adds the pause happy-path + core-parity, the CLI Δt=0-matches-no-flag pin, exit-1 (neg Δ / off-track distil-Teff / bd+Δ), exit-2 (non-numeric Δ).
- `test_thermal.py`, `test_query_thermal.py` — the **Phase V** power/thermal/shielding calculators (`core/thermal.py` + `core/shielding_tables.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo's Packet 13, Phase-N/T/U lineage (no GUI, no network, no DB). Fulfils **Group F** of the calculator-extensions request. `test_thermal.py` (in-process): **F1 `compute_waste_heat`** (3 GW @ η=0.4 → useful 1.2e9 / waste 1.8e9; the Carnot ceiling T_hot=1500/T_cold=300 → η_carnot=0.8 and the `carnot_limited` flag on a claimed η=0.9; `Q_min` floor; validation matrix), **F2 `compute_radiator_area`** (the σT⁴ black-side anchors 459 W/m²@300K / 5.67e4@1000K; the headline **1 GW @300K, ε=0.9, double-sided → ≈1.21 km²**; the A∝T⁻⁴ 16× scaling; the inline F1 chain; sides/mass; the warm-sink flux collapse; `T_sink≥T_rad`→error), **F3 `compute_shielding_attenuation`** (photon: water@1MeV → HVL≈9.8 cm / TVL≈32.6 cm / 20 g/cm²→0.243, lead@1MeV linear HVL≈0.86 cm, explicit-coeff, nearest-energy flagging, the hydrogen alias + per-gram note, the **table-closure check over every bundled (material,energy)** HVL/TVL↔μ/ρ, the thickness↔areal-density parity; gcr: the `is_order_of_magnitude`+`buildup_caveat` fields, explicit Λ; the validation matrix) + determinism. `test_query_thermal.py` is the offline `query.py` subprocess contract (mirrors `test_query_cooling_hz.py`): happy-path JSON + core-parity for all three, the curated-`{"error"}` exit-1 matrix (η>1 / no efficiency anchor / `T_hot≤T_cold` / `T_sink≥T_rad` / no heat anchor / off-grid material / no energy / no bundled GCR Λ) and the argparse exit-2 matrix (missing power group, mutex-both, non-numeric, missing required `--radiator-temp-k`, bad `--sides`/`--mode` choice). **Phase AD extensions** (C6/C7/C9): **C6** `shielding-attenuation --particle {proton,alpha,ion}` → CSDA range (bundled NIST PSTAR proton/water grid; 100 MeV → **7.718 g/cm²** golden pin + monotonicity closure; `stops_primary`/`residual_range_gcm2`; `--csda-range-gcm2` override for alpha/ion/other materials — a surfaced data-scope decision; photon default byte-identical regression pin); **C7** `--layers "mat:gcm2,…"` multi-layer product (single-layer == single-material parity, order-independence); **C9** `waste-heat` transient mode (all six of `--peak-w/--mean-w/--duty/--pulse-period-s/--storage-mass-kg/--specific-heat-jkgk`; `temp_swing_k`/`buffer_time_s`; steady-state carries no `mode` key = regression pin; the power mutex group is no longer argparse-`required` so `waste-heat` with no anchor is exit-1 not exit-2). Also `test_active_shield.py`/`test_query_active_shield.py` (**C8** — see below).
- `test_spin.py`, `test_query_spin.py` — the **Phase W** rotating-habitat comfort calculator (`core/spin.py` + `core/spin_tables.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo's Packet 14, Phase-N/T/U/V lineage (no GUI, no network, no DB). The in-house **SpinCalc** analog: given exactly two of {radius, rpm, gravity, rim velocity} it solves the rest + the three comfort quantities the `gravity-*` solves lack (rim tangential velocity, head-to-foot gravity gradient, Coriolis ratio) + a tiered comfort verdict. `test_spin.py` (in-process): the request's **acceptance cases A/B/C** (224 m @ 2 rpm → 1.0019 g / 46.9 m/s / 0.80 % gradient / 4.26 % Coriolis, conservative PASS; 10 m @ 1 g → 9.46 rpm / 18 % gradient, all tiers fail; 56 m @ 4 rpm → conservative fail / moderate+relaxed pass) — locking the two spec reconciliations (Case C moderate PASSES 1.0019 g via the 1 % `_BAND_TOL`; Case B Coriolis 20.2 % < 25 % PASSES while the tier still fails on rpm+gradient); all **six anchor pairings** recovering the same (ω,r); the solve-consistency case (6 m/s floor at 1 g → r 3.67 m / 15.6 rpm); the validation matrix (not-exactly-two anchors, h≥ solved r, non-positive anchor/height/walk-speed, bad override/percentage, bad criteria); overrides flipping a verdict + `overridden_thresholds`; single-tier selection; null-threshold `pass:null`; determinism; and the **bundled-band golden pin** (the request's numbers verbatim). `test_query_spin.py` is the offline `query.py spin-comfort` subprocess contract (mirrors `test_query_thermal.py`): happy-path JSON + core-parity + the `--gravity-g` form + single-tier, the curated-`{"error"}` exit-1 matrix (non-positive anchor / h≥r / one anchor / three anchors / bad override) and the argparse exit-2 matrix (`--gravity-g`+`--accel-ms2` mutex, bad `--criteria`, non-numeric).
- `test_life_support.py`, `test_query_life_support.py` — the **Phase X** closed-loop life-support & bioregenerative calculators (`core/life_support.py` + `core/life_support_tables.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo's Packet 15, Phase-N/T/U/V/W lineage (no GUI, no network, no DB, no RNG). Three `query.py`-only subcommands over one bundled static table (**NASA BVAD Rev2**, NASA/TP-2015-218570/REV2 Feb 2022, Tables 3-31/4-20/4-90/4-91, transcribed verbatim; MELiSSA/PBR algae flagged distinct). `test_life_support.py` (in-process): the bundled-table integrity (BVAD Rev2 rates; every crop row; the PAR photon energy `0.2177 J/µmol` within ~1 % of the first-principles `h·c·N_A/550nm`; closure scenarios ∈ [0,1]), and the acceptance anchors — **X1 `compute_life_support`** (per person / open / 1 day = the Rev2 set O₂ 0.895 / CO₂ 1.085 / food 0.800 / 3054 kcal / potable 2.0; linear crew×days scaling; ISS-365 water makeup = 0.10× the open total; `--kcal-per-day` scales energy not O₂; the older-textbook `--o2-rate 0.816 --food-dry-rate 0.617 --kcal-per-day 2500` override; per-stream closure override), **X2 `compute_bioregen_area`** (2500 kcal/DLI 30/wheat → area ∈ 30–50 m² [≈ 40] with the measured cross-check within a factor ~2; `--artificial --led-par-efficiency 0.4` → 5–15 kW/person; the three light anchors PPFD/DLI/PAR-W·m⁻² agreeing; algae smaller area + productivity path + gas exchange; generic-HI-required-without-crop; gas/transpiration scaling with crew), **X3 `compute_population_capacity`** (power-bound → 100; a tight fixed-N budget flips `binding_constraint` + reports slack; default-vs-flag `source`; omitted resources not counted) — plus the validation matrix (non-positive crew/days/area/power/rate; closure ∉ [0,1]; efficiency/HI/f_edible ∉ (0,1]; unknown crop/scenario; zero/two X2 light anchors; no X3 budget) and determinism. The X2 default `η_photo` is **0.10** (biomass-energy/incident-PAR; RUE-derived, not the 0.03 first drafted — see `PHASE_X_PLAN.md` §1 D2). `test_query_life_support.py` is the offline `query.py` subprocess contract (mirrors `test_query_spin.py`): happy-path JSON + core-parity for all three, the curated-`{"error"}` exit-1 matrix (non-positive crew/days; closure out of range; no crop/no HI; non-positive anchor; no X3 budget) and the argparse exit-2 matrix (bad `--closure-scenario`/`--crop` choice; missing/two X2 light anchors; a rejected `--star` on `bioregen-area` → unknown-arg; non-numeric). **Phase AD (C10)** adds `bioregen-area --crops "wheat:0.5, white_potato:0.3, soybean:0.2"` (a calorie-split diet mix; mutually exclusive with `--crop`): per-crop area at each crop's HI/productivity → `per_crop_area_m2[]` + `area_m2_total=Σ`; fractions must sum to 1.0 (the tool **rejects, not normalizes** — Locked C10), and the protein/vitamin-target diet LP is a surfaced v2 decision noted in `model_note`. Tests: single-entry `"wheat:1.0"` == `--crop wheat` area parity, the calorie-weighted per-crop sum (each = single-crop area × its fraction), the LP-deferral note, and the validation matrix (not-summing-1 / unknown crop / malformed token / bad fraction / negative fraction / empty / both `--crop`+`--crops`); query happy-path + core-parity + the exit-1 mix matrix.
- `test_propulsion.py`, `test_query_propulsion.py` — the **Phase Y** STL mission energetics (`core/propulsion.py` + `core/propulsion_tables.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo's Packet 16 (Group G of the combined settlement/propulsion/astrobiology/terraforming request), Phase-N/T/U/V/W/X lineage (no GUI, no network, no DB, no RNG). Two `query.py`-only subcommands adding the mass/energy side of STL travel (`query.py` had only kinematics). `test_propulsion.py` (in-process): the acceptance anchors — **G1 `compute_rocket_equation`** (classical Δv30/v_e30 → MR≈2.718 / frac 0.632; relativistic β0.1/v_e0.1c → MR≈2.73 flyby & 7.44 rendezvous; `fusion-dt` β0.1 → MR≈28 flyby / ~804 rendezvous — the "marginal generation ship"; photon v_e=c → MR≈1.105; round-trip = MR⁴), all three two-of-{velocity,exhaust,mass_ratio} solve forms, the `--isp-s` and `--fuel` exhaust anchors, the relativistic-flag emit-β behavior, and the payload mass budget; **G2 `compute_beam_sail`** (100 GW reflective R=1 → F≈667 N = 2P/c, absorptive → P/c, areal-mass path, final velocity over accel time/distance, diffraction beam-range note) — plus the validation matrix (wrong anchor count / β∉[0,1) / MR≤1 / unknown fuel / `--relativistic`+`--delta-v-kms` / reflectivity∉[0,1] / both accel modes / one of wavelength·aperture) and the fuel-preset golden pin + determinism. **`fusion-dt` is bundled at 0.03c** (effective) — the request's acceptance anchor requires it (0.05c gives only MR≈7.4); the tension with the request's "~0.05–0.09c ideal" prose is documented in the preset note. `_C_MS` was promoted to `core/equations.py` (single source of truth; `test_calculators.py`/`test_equations.py`/route suites guard the move). `test_query_propulsion.py` is the offline `query.py` subprocess contract (mirrors `test_query_thermal.py`): happy-path JSON + core-parity for both, the curated-`{"error"}` exit-1 matrix (one anchor / β=1 / MR<1 / rel+Δv / power 0 / reflectivity>1 / no mass source) and the argparse exit-2 matrix (bad `--fuel`/`--legs` choice; missing required `--beam-power-w`; the accel mutex; non-numeric). **Phase AD (C2)** adds `pellet-stream` (`compute_pellet_stream`, the mass analog of `ramscoop`): closing velocity `u=v_s−v` → thrust `F=g·ṁ·u` (g=2 reflect / 1 absorb) → `drive`/`no-thrust` verdict at the `v=v_s` crossover. Tests: the drive anchor (`v_s 30000, ṁ 1, β 0.05, reflect` → u≈15010 km/s / F≈3.0×10⁷ N), the `v=v_s`/`v>v_s` **no-thrust clean-negative** (exit 0), `absorb`=half `reflect`, the two mass-flow anchors (`--mass-flow-rate-kgs` == `--pellet-mass-kg`·`--pellet-rate-hz`), `--velocity-kms 0` allowed, optional `acceleration_ms2`, the validation matrix + determinism; query exit-1 (v_s≤0 / no or partial mass anchor / no velocity / β=1 / mass≤0) + exit-2 (velocity mutex, mass-flow mutex, bad `--coupling`, missing required `--stream-velocity-kms`, non-numeric).
- `test_megastructure.py`, `test_query_megastructure.py` — the **Phase Z** megastructure-scale calculators (`core/megastructure.py` + `core/materials_tables.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo's Packet 17 (Group H of the combined settlement/propulsion/astrobiology/terraforming request), Phase-N/T/U/V/W/X/Y lineage (no GUI, no DB, no RNG; network only on `dyson-collector --star`). Three `query.py`-only subcommands giving the material size limit that pairs with `spin-comfort`'s human-comfort minimum. `test_megastructure.py` (in-process): the acceptance anchors — **H1 `compute_spin_stress`** (steel SF1/1 g → v_max ≈ 226 m/s / r_max ≈ 5.2 km; carbon-fiber → 1580 m/s / 254 km; the radius→max-gravity and rpm+radius→hoop-stress+margin forms; explicit ρ/σ == the bundled material; SF 3 default shrinks 3×), **H2 `compute_tether_taper`** (the Pearson uniform-stress closed form validated: Earth steel → overflow/infeasible; Earth CNT@100 GPa SF1 → taper ≈ 1.9, the canonical "modest taper" result; graphene ≈ 2.3; kevlar ≫10; explicit body == bundled Earth; the Moon Hill-sphere caveat), **H3 `compute_dyson_collector`** (Sun f0.01/1 AU → P ≈ 3.83e24 W / area ≈ 2.81e21 m² / incident flux 1361 W/m²; mass + area scaling) — plus the validation matrix (unknown material/body; ρ/σ/g/R ≤ 0; SF<1; no/two solve forms; material+explicit both; rpm without radius; geo≤surface; fraction∉(0,1]) and the bundled-table golden pins (`_MATERIALS` incl. the researched CNT 100 GPa / graphene 130 GPa theoretical values + the brittle/raw-filament/MTA flags; `_BODIES`) + determinism. `test_query_megastructure.py` is the offline `query.py` subprocess contract (mirrors `test_query_propulsion.py`): happy-path JSON + core-parity for all three, the infeasible-taper-is-exit-0 case, the curated-`{"error"}` exit-1 matrix (no solve form / SF<1 / material+explicit / no body / body+explicit / fraction>1) and the argparse exit-2 matrix (bad `--material`/`--body` choice; the `dyson-collector` luminosity/star mutex; missing required `--fraction`; non-numeric). **Phase AD (C4)** adds `orbital-ring` (`compute_orbital_ring`): a stationary ring held up by a faster-than-orbital rotor — local gravity `g(r)=g₀·(R/r)²`, `v_orb=√(g·r)`, `v_rotor=√(r·g·(1+λ_ring/λ_rotor))` (support_ratio=λ_ring/λ_rotor) over the bundled `_BODIES` `g₀`/`R_km` (no table edit). Tests: the Earth/alt-300 anchor (g≈8.95, v_orb≈7.73, v_rotor≈10.93 km/s, ratio √2), the doubling-λ_ring→√1.5× rotor-velocity scaling, explicit-body parity, the `rotor_ke_per_length_jm` cross-check, the validation matrix + determinism; query exit-1 (no/both/partial body anchor, ring-λ≤0) + exit-2 (bad `--body` choice, missing required `--altitude-km`/`--ring-mass-per-length-kgm`, non-numeric).
- `test_volatile_delivery.py`, `test_query_volatile_delivery.py` — the **Phase AD (C5)** volatile-delivery / cometary-bombardment calculator (`core/volatile_delivery.py`), offline; `query.py`-only, the **supply** side complementing Phase AB's `atmosphere-mass` demand side. One `query.py volatile-delivery` subcommand composing three durable pieces: delivered volatile mass `m_vol=f·M`, the redirect burn's classical Tsiolkovsky mass ratio (`MR=exp(Δv/v_e)` via `propulsion.compute_rocket_equation` + its bundled fuel presets), impact energy `½·M·v_impact²` (+ TNT-equivalent `E/4.184e6` kg), and bodies needed `N=M_atm_target/m_vol`. Each add-on's outputs are `null` when its input is omitted. `test_volatile_delivery.py` (in-process): the anchor (M 1e15/f 0.5/v 20 → delivered 5×10¹⁴ kg / E≈2×10²³ J / target 5.15×10¹⁸ → bodies≈10 300), the redirect MR from `--fuel fusion-dt` (≈1.0001, core-parity vs `compute_rocket_equation`) and explicit `--exhaust-velocity-kms`, the add-on null behaviour, fraction-scaling, the validation matrix (M≤0 / fraction∉(0,1] / Δv≤0 / v≤0 / target≤0 / Δv without or with two exhaust anchors / fuel·v_e without Δv / unknown fuel) + determinism. `test_query_volatile_delivery.py` is the offline subprocess contract (mirrors `test_query_megastructure.py`): happy-path + core-parity, the redirect-MR case, the exit-1 matrix + exit-2 (bad `--fuel` choice, missing required `--body-mass-kg`, non-numeric). See `PHASE_AD_PLAN.md`, `docs/integration.md` (volatile-delivery).
- `test_par_flux.py`, `test_query_par_flux.py` — the **Phase AA** PAR / photosynthesis-by-stellar-type calculator (`core/par_flux.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo's Packet 18 (Group I of the combined settlement/propulsion/astrobiology/terraforming request), Phase-N/T/U/V/W/X/Y/Z lineage (no GUI, no DB, no RNG, no time; network only on `par-flux --star`). **No bundled table** — the SED is the **Planck blackbody at Teff** (flagged an approximation in `sed_model`/`model_note`); the only new constants are `_PLANCK_H` / `_AVOGADRO` / `_SOLAR_LUMINOSITY_W` in `core/equations.py` (reusing Phase-Y's `_C_MS`, plus `_K_B`/`_STEFAN_BOLTZMANN`/`_M_PER_AU`). One `query.py`-only subcommand `par-flux` computing the PAR fraction (`∫₄₀₀⁷⁰⁰ B_λ / (σT⁴/π)`, composite-Simpson), PAR irradiance, **PPFD** (band-mean photon energy → µmol·m⁻²·s⁻¹, cross-checked against ≈0.219 J/µmol), and the red-dwarf **deficit vs G2** (`f_PAR(5772)/f_PAR(Teff)`). Its PPFD **feeds** Phase-X `bioregen-area`'s PAR input (`feeds_note`). `test_par_flux.py` (in-process): the **Sun anchor** (f_PAR∈[0.36,0.40], PAR=S·f_PAR, PPFD>2000, deficit≈1, the 0.219 J/µmol cross-check to ~3 %), the **M-dwarf anchor** (blackbody reproduces the plan's f_PAR 0.04–0.07 / deficit 6–10 band at **Teff 2700 K**, a late-M dwarf — a **3000 K blackbody honestly gives ≈0.081**, the real-SED gap being documented in `model_note`; blackbody is *optimistic* for red dwarfs), the three Teff sources (`--teff-k`; `--spectral-type` via the `main_sequence_stars` ceiling rule matching the direct path; `--star` mocked + SIMBAD-error passthrough), the two insolation sources (`--luminosity-lsun`+`--distance-au` → 1361 W/m² at 1 L☉/1 AU, inverse-square, parity with the direct path), the band override (400→750 raises f_PAR), the validation matrix (teff/insolation ≤ 0, 0/2 Teff or insolation sources, band lo≥hi / ≤0, partial lum+dist, unresolvable spectral type) and determinism. `test_query_par_flux.py` is the offline `query.py par-flux` subprocess contract (mirrors `test_query_thermal.py`): happy-path JSON + core-parity, the spectral-type auto-seed path, the M-dwarf/band-override cases, the curated-`{"error"}` exit-1 matrix (teff 0 / two Teff / no insolation / no Teff / distance 0 / band lo≥hi) and the argparse exit-2 matrix (non-numeric `--teff-k`, malformed `--par-band-nm`); the `--star` SIMBAD round-trip is reachability-gated (`RUN_NETWORK_TESTS`). **Phase AD (C1)** adds `par-flux --sed {blackbody,real}` (default **blackbody** — a user-decided deviation from the plan's "default real", to keep the consumer's existing output backward-compatible; real is opt-in) backed by the new **`core/par_flux_tables.py`** `_REAL_SED_FPAR` table (BT-Settl CIFIST2011, log g 4.5, [M/H] 0; `f_PAR = ∫400–700nm F_λ / ∫F_λ` **computed at build from the SVO Theoretical Spectra grid** — Allard+ 2012 / Baraffe+ 2015 — captured in `_SOURCE`), linear-interpolated in Teff; band-fixed at 400–700 nm, grid 2600–7000 K. `test_par_flux.py` C1 tests: default-is-blackbody (bare == `--sed blackbody`), **3000 K real ≈ 0.0228 < blackbody 0.081** (larger deficit 17×), **Sun real ≈ 0.389 > blackbody 0.366** (matches ASTM ~0.39 — the plan's 0.40–0.45 was slightly high), the interpolation midpoint, the band-fixed + off-grid error paths, and the table golden pin (pinned nodes + monotonicity + `real_f_par` bounds). `test_query_par_flux.py` adds the `--sed real` happy-path + core-parity, default-blackbody, and the exit-1 (band-fixed / off-grid) + exit-2 (bad `--sed`) cases.
- `test_terraforming.py`, `test_query_terraforming.py` — the **Phase AB** planetary energy-balance / terraforming calculators (`core/terraforming.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo's Packet 19 (**Group J** — the **final group** of the combined settlement/propulsion/astrobiology/terraforming request), Phase-N/T/U/V/W/X/Y/Z/AA lineage (no GUI, no DB, no RNG, no time, no network). Three `query.py`-only subcommands modelling the terraforming **radiative/mass balance** (demand side only). **No bundled table** — reuses `_STEFAN_BOLTZMANN`/`_M_PER_AU`/`_G`/`_EARTH_MASS_KG`/`_SOLAR_LUMINOSITY_W` from `core/equations.py` + one inline `_EARTH_ATM_MASS_KG = 5.15e18`; the `--luminosity-lsun`+`--distance-au`→S conversion is shared with Phase-AA. `test_terraforming.py` (in-process): **J1 `compute_equilibrium_temp`** (`T_eq=[S(1−A)/4σ]^¼`; the Earth S1361/A0.3→255 K, +Δ33→288 K + Mars S589/A0.25→210 K anchors; the three forcing forms — additive offset / grey-atmosphere `T_eq(1+¾τ)^¼` with τ≈0.851→288 K / the **inverse** `--target-surface-k`→required ΔT≈33 & τ≈0.85, plus the `cooling_required` flag when the target is below equilibrium; lum+dist source parity → 1361 W/m²), **J2 `compute_insolation_shift`** (`A_m=|ΔS|·4πR²/solar_flux`; mirror/shade `mode` by sign with equal areas; the Mars 4πR²≈1.44e14 order-of-mag + the exact `4·|ΔS|/solar_flux` cross-section ratio), **J3 `compute_atmosphere_mass`** (`m=4πR²P/g`; Mars 1 bar/R3390/g3.71→3.9e18 kg / 0.76 Earth-atm; the mass↔pressure round-trip; g-from-`planet_mass_earth` 0.107→3.71 matching an explicit g; the echoed `species` label) — plus the validation matrix (albedo∉[0,1); **>1** forcing forms [0 is valid — the Phase-AD airless fix] or 0/2 insolation/flux/gravity sources; τ<0; target≤0; ΔS=0; radius=0; two of pressure/mass; unknown species) and determinism. `test_query_terraforming.py` is the offline `query.py` subprocess contract (mirrors `test_query_thermal.py`): happy-path JSON + core-parity for all three, the inverse & shade-sign cases, the curated-`{"error"}` exit-1 matrix (albedo 1.0 / >1 forcing / no insolation / ΔS=0 / no P·m / two gravity) and the argparse exit-2 matrix (non-numeric, bad `--species` choice, missing required `--planet-radius-km`). **Phase AD (B):** `compute_equilibrium_temp` now accepts **zero** forcing forms → the bare airless equilibrium (`t_surface_k = t_eq_k`, `regime:"airless"`; Earth 1361/0.3 → 254.6 K, Mars 589/0.25 → 210.1 K); a `regime` field tags every result (`airless`/`offset`/`grey`/`inverse`) and the existing forcing branches are regression-pinned (only **>1** forcing form now errors).
- `test_ism_drag.py`, `test_query_ism_drag.py` — the **Phase AC** ISM-drag / magnetic-sail calculators (`core/ism_drag.py` + `core/ism_drag_tables.py`), offline; for the sibling `scifiWorldBuilding-Claude` repo's Packet 16 STL Colonization Propulsion (**Group K** of the combined request — the magnetic ISM interaction, the one scope-shaping STL gap Groups G–J did not enumerate), Phase-N/T/U/V/W/X/Y/Z/AA/AB lineage (no GUI, no DB, no RNG, no time, no network). Two `query.py`-only subcommands over one bundled ISM/fusion constant table (isolated, like `core/propulsion_tables.py`); `_MU_0`/`_M_PROTON` added to `core/equations.py`. `test_ism_drag.py` (in-process): **K1 `compute_magsail`** (magnetopause standoff `R_mp=[μ₀m_dip²/(8π²kρv²)]^(1/6)`, drag `F=C_d·½ρv²·πR_mp²`; the request anchor n0.1/m1.0/β0.1/R_coil 1e5/I 1e5 → R_mp≈101 km / drag≈2.38 kN / a≈2.4e-3 m/s² @10³ t; the `F ∝ v^(4/3)` scaling via β-halving → 2^(4/3)≈2.52× and `R_mp ∝ v^(−1/3)`; moment↔coil equivalence; the explicit standoff formula; the analytic single-law stopping distance/time, slower-target→longer tail; near-field warning; LIC defaults; determinism) + the validation matrix; **K2 `compute_ramscoop`** (ideal p-p `v_e=c√(2f)`≈0.1192c; default η=0.1 → brake; **the Zubrin & Andrews result** — the ideal η=1 `v_e>v` reaction margin exists yet magnetic drag flips it to brake; low-β+high-η → drive; the analytic crossover `v_e/(1+C_d/2)`; explicit-`v_e` and mass-flux/force forms) + the validation matrix; plus the bundled-table golden pins (p-p/CNO 0.71% verifiable from 26.73 MeV / 4·938.27 MeV, D-D 0.38%, the LIC/coefficient defaults). `test_query_ism_drag.py` is the offline `query.py` subprocess contract (mirrors `test_query_propulsion.py`): happy-path JSON + core-parity for both (incl. K1 stopping and K2 brake/drive), the curated-`{"error"}` exit-1 matrix (no/partial/double anchors, β=1, `--velocity-final-kms` without mass, η>1, scoop area+field, no exhaust) and the argparse exit-2 matrix (the `--velocity-kms`/`--beta` mutex, bad `--fuel` choice, non-numeric). **Coefficient provenance** (web-confirmed 2026-07-02): C_d=1.0 (Zubrin & Andrews explicit), k=1.0 (simple pressure balance; compressed factor f=2/2.44), fusion p-p/CNO 0.71% / D-D 0.38% (reconciled down from the request's stated 0.43% — flagged to the requester), η 0.1 low; ISM density is a caller parameter (defaults flag to the sibling Local-Interstellar-Environment packet: n 0.1 cm⁻³ LIC, ion mass 1.3 amu), and every output carries an `ionization_note` (the LIC is only ~22% ionized → "fully ionized" overestimates the interacting density ~4×). See `PHASE_AC_PLAN.md`, `docs/integration.md` (ISM drag / magnetic sail). **Phase AD (A1/A2/A3):** the magsail **coil-pair** anchor now uses the **exact on-axis loop field** (`R_mp=√([μ₀I²R⁴/(8kρv²)]^⅓−R²)`, algebraic; clamps to R_coil with an informational note when the ram pressure exceeds the peak central field), while the **moment-only** anchor keeps the far-field dipole — both echoed (`magnetopause_radius_km` vs `magnetopause_radius_farfield_km`); the R_mp≈101 km/2.38 kN headline is now the moment-only anchor (byte-identical to AC), the same coil I/R gives the exact near-field ≈13 km. A3 adds `--ionization-fraction` (default 1.0, byte-identical regression pin; 0.5 halves ρ → halves ramscoop fixed-area drag/mass-flux) to both `compute_magsail`/`compute_ramscoop`; A2 rewords the magsail note (constant-ISM stopping distance/time are exact). Tests updated: coil-exact + far-field-agreement (R_mp≳3·R_coil → <1%) + ionization cases. **Phase AL (R5)** adds `FissionTableTest` — the new `_FISSION` mass→energy fractions beside `_FUSION`: u235 f=**9.14e-4** (verifiable 200 MeV / (235.04·931.494)) and its **f·c²≈8.21e13 J/kg** storage ceiling, pu239 f=9.3e-4, and the antineutrino-loss flag in every row note.
- `test_active_shield.py`, `test_query_active_shield.py` — the **Phase AD (C8)** active magnetic-shield rigidity-cutoff calculator (`core/active_shield.py`), offline; `query.py`-only, Phase-N/T/V lineage (no GUI/DB/network/RNG). One `query.py active-shield` subcommand: the **Störmer equatorial cutoff** `R_c=(μ₀c/16π)·m/r²` (the constant ≈7.495 reproduces Earth's ≈14.8 GV geomagnetic cutoff — anchored cross-check) from a dipole moment / coil pair / field×scale (exactly one field source) + a required `--shield-radius-m`, plus an order-of-magnitude deflected fraction `1−exp(−R_c/R_s)` and the equatorial field `μ₀m/4πr³`. Reuses `_MU_0`/`_C_MS`. Self-validating (curated `{"error"}` exit 1 for radius≤0 / not-exactly-one field source / non-positive params / R_s≤0; argparse exit 2 for missing required `--shield-radius-m` / non-numeric). The field side of shielding, complementing `shielding-attenuation`'s mass side. See `PHASE_AD_PLAN.md`, `docs/integration.md` (active-shield).
- `test_dust_impact.py`, `test_query_dust_impact.py` — the **Phase AD (C3)** hypervelocity dust/ISM-grain impact-energetics calculator (`core/dust_impact.py`), offline; `query.py`-only, Phase-N/T/V lineage (no GUI/DB/network/RNG). One `query.py dust-impact` subcommand: grain mass `m=(4/3)πr³ρ` (or explicit) → kinetic energy `½mv²` switching to the relativistic `(γ−1)mc²` once **β>0.1** (the `relativistic` flag; the plan prose said ~0.01 but its own cases require `false@β0.05`/`true@β0.2`) → TNT-equivalent `E/4.184e6` kg → momentum `mv`→`γmv` → optional cumulative impacts `N=n·A·L` + energy fluence `N·E/A` over an ISM column. **Penetration depth is deliberately not computed** (`penetration_handoff_note` → the Packet-13 `shielding-attenuation`/`radiator-area` tools). Reuses `_C_MS`/`_SEC_PER_YEAR` (`_LY_M` local, matching `ism_drag`). `test_dust_impact.py` (in-process): the corrected 1 µm / 1000 kg·m⁻³ / β 0.1 anchor (**m≈4.19×10⁻¹⁵ kg, E≈1.88 J, TNT≈0.45 mg** — the plan's "1.9×10² J / 40 mg" were computed at v=c, and its `E/4.184e12` divisor is kilotons not kg; both corrected), the geometry-vs-explicit-mass parity, the β>0.1 relativistic auto-switch (γ/E/momentum), the cumulative set present/null + linear scaling in path & density, the **no-penetration-key + handoff-note** assertion, the validation matrix + determinism. `test_query_dust_impact.py` is the offline subprocess contract (mirrors `test_query_propulsion.py`): happy-path + core-parity, the relativistic+cumulative case, the exit-1 matrix (no grain anchor / radius w/o density / no velocity / β=1 / partial cumulative) + exit-2 (grain mutex, velocity mutex, non-numeric). See `PHASE_AD_PLAN.md`, `docs/integration.md` (Momentum / impact tools).
- `test_astro_bodies.py` — the **Phase 0 (AE–AI)** shared mass/radius/body-preset resolver (`core/astro_bodies.py`), offline (no network/Qt/DB). The exactly-one multi-unit gate + positivity for `resolve_mass` (kg/msun/mearth/mjup) and `resolve_radius` (m/rsun/rearth/au), the unit conversions, the custom-name error message (for K3's "body mass"/"primary mass"), the `_BODY_PRESETS`/`_OBJECT_PRESETS` tables (Earth mass+radius, Sgr A* mass, sorted key lists incl. jupiter/m87-star), the returned-dict-is-a-copy guard, and the `_HBAR = _PLANCK_H/2π` derivation. Underpins Groups K–O; see `PHASE_AE_PLAN.md`.
- `test_gravitation.py`, `test_query_gravitation.py` — the **Phase AE (Group K)** arrival-geometry / gravitation calculators (`core/gravitation.py`), offline; `query.py`-only, Phase-N/T/V lineage (no GUI/DB/network/RNG). Four subcommands, all resolving mass/radius through `core/astro_bodies.py`: **K1 `escape-velocity`** (`v_esc=√(2GM/r)`, `v_circ=v_esc/√2`, `½v_esc²`; anchors Earth **11.19**, Sun **617.7**, Jupiter **59.5** km/s, Earth specific energy **6.26×10⁷ J/kg**; explicit==preset parity), **K2 `gravitational-potential`** (`Φ=−GM/r`, well-depth `GM(1/r_from−1/r_to)` default r_to=∞, payload binding energy, Δv=√(2|well|); anchors Earth surf→∞ **6.26×10⁷**, Sun **1.91×10¹¹ J/kg**; finite-r_to < ∞ check), **K3 `sphere-of-influence`** (Laplace `a·(m/M)^0.4` beside Hill `a·(m/3M)^⅓`; anchors Earth-about-Sun SOI **0.00618 AU / 0.924×10⁶ km**, Jupiter SOI **4.82×10⁷ km**; ratio), **K4 `hyperbolic-approach`** (`v_p=√(v∞²+2GM/r_p)`, C₃=v∞², capture Δv for circular/parabolic/elliptical; anchor v∞=3 km/s @Earth r_p=6771 km → v_p **11.26**, capture Δv **3.59** km/s; the two v-modes' parity, target ordering, `--periapsis-rbody`). `test_gravitation.py` (in-process): anchors + the self-validating error matrix (mass/radius gate, `--body`+explicit conflict, missing semimajor, both/neither v-mode, both/neither periapsis, rbody-without-radius, below-escape "bound not hyperbolic", elliptical without/too-small apoapsis). `test_query_gravitation.py` is the offline subprocess contract (mirrors `test_query_active_shield.py`): happy-path + core-parity for all four, the curated exit-1 matrix, and the argparse exit-2 matrix (bad `--body`/`--target` choice, non-numeric). See `PHASE_AE_PLAN.md`, `docs/integration.md` (Arrival geometry & gravitation).
- `test_relativity.py`, `test_query_relativity.py` — the **Phase AF (Group L)** special-relativity + causality calculators (`core/relativity.py`), offline; `query.py`-only, Phase-N/T/V lineage (no GUI/DB/network/RNG). Eight subcommands extending the `lorentz-factor` seed: **L1 `time-dilation`** (special γ 2.0@0.866 / 7.089@0.99 + proper↔coordinate solve; gravitational `√(1−r_s/r)` Earth **1−6.95×10⁻¹⁰**; `--combined` product; radius≤r_s error), **L2 `length-contraction`** (0.5 L₀@0.866, inverse solve), **L3 `velocity-addition`** (collinear 0.75⊕0.75=**0.96**, c⊕x=c with null γ, perpendicular sublight), **L4 `relativistic-doppler`** (approach **2.0**/z=−0.5, transverse **0.8**, recede 0.5, λ/f shifts), **L5 `rapidity`** (φ 0.6931@0.6, `--add` compose 3× → φ 2.079 / **0.9695c**), **L6 `relativistic-energy-momentum`** (proton@0.99 → γ 7.089 / KE **5.72 GeV**; velocity/γ/KE/momentum state equivalence; mass-mev↔kg), **L7 `lorentz-transform`** (β=0.6 event(0,1ly) → t' **−0.75yr**/x' **1.25ly**, inverse round-trip, simultaneity offset, mixed-unit guard), **L8 `causality-check`** (u·v>c² loop: 2c·0.6c=1.2 **true**/v_crit 0.5, 2c·0.4c=0.8 false, `--instant` any v>0 true, `--preferred-frame` safe). `test_relativity.py` (in-process): anchors + the self-validating error matrix per function. `test_query_relativity.py` is the offline subprocess contract (mirrors `test_query_active_shield.py`): happy-path + core-parity, the `--add`/`--event2` string-parsing wrappers (incl. a malformed-`--add` exit-1), the curated exit-1 matrix, and the argparse exit-2 matrix (missing required `--u-c`/`--v-c`/`--velocity-c`, non-numeric). See `PHASE_AE_PLAN.md`, `docs/integration.md` (Special relativity & causality).
- `test_exotic_physics.py`, `test_query_exotic_physics.py` — the **Phase AG (Group M)** exotic-vacuum / cosmology calculators (`core/exotic_physics.py`), offline; `query.py`-only, Phase-N/T/V lineage (no GUI/DB/network/RNG). Four subcommands: **M1 `casimir`** (parallel-plate `P=π²ℏc/240d⁴` → **1.30×10⁻³ Pa**@1µm / **1.30×10⁵**@10nm, negative `u=−π²ℏc/720d³` → **−4.33×10⁻¹⁰ J/m³**, the 1/d⁴ scaling + area×force parity; sphere-plate PFA force with null pressure/energy), **M2 `vacuum-energy`** (ρ_Λ **5.3×10⁻¹⁰** / ρ_crit **7.7×10⁻¹⁰ J/m³**, Λ **1.09×10⁻⁵² m⁻²**, w=−1, QED cutoff `E⁴/(ℏc)³` Planck ratio ~**10¹²²**; electroweak<planck ratio ordering + custom-GeV cutoff), **M3 `schwinger-limit`** (E_c **1.32×10¹⁸ V/m** / B_c **4.41×10⁹ T** / I_c **2.3×10²⁹ W/cm²** via ½ε₀cE²; ratio-to-critical ≈1 at E_c for both field & intensity), **M4 `hubble-flow`** (recession v=H₀d **6740 km/s**@100Mpc; binding turnaround `(GM/Ω_ΛH₀²)^⅓` → Local Group bound r_ta **1.6 Mpc**, galaxy bound ratio ≫1, small-mass-far-out unbound). `test_exotic_physics.py` (in-process): anchors + the self-validating error matrix. `test_query_exotic_physics.py` is the offline subprocess contract (mirrors `test_query_active_shield.py`): happy-path + core-parity, the curated exit-1 matrix (no separation / sphere-plate no radius / Ω_Λ>1 / bad cutoff string / both schwinger flags / hubble both-or-neither mode), and the argparse exit-2 matrix (bad `--geometry` choice, non-numeric). See `PHASE_AE_PLAN.md`, `docs/integration.md` (Exotic vacuum & cosmology).
- `test_black_hole.py`, `test_query_black_hole.py` — the **Phase AI (Group O)** black-hole / relativistic-thermodynamics calculators (`core/black_hole.py`), offline; `query.py`-only, Phase-N/T/V lineage (no GUI/DB/network/RNG). Ten subcommands, mass via `core/astro_bodies.py` multi-unit flags or `--object` presets (Sgr A*, M87*, TON 618): **O1 `schwarzschild-radius`** (Sun **2.953 km** / Earth 8.87 mm / Sgr A* **0.082 AU**), **O2 `hawking-temperature`** (1 M☉ **6.17×10⁻⁸ K**, inverse T→M **4.5×10²²** @CMB + round-trip), **O3 `black-hole-evaporation`** (τ **2.1×10⁶⁷ yr** / P 9.0×10⁻²⁹ W @1 M☉, inverse τ→M **1.73×10¹¹ kg** @age-of-universe; photon-only 5120π coefficient noted), **O4 `bekenstein-hawking-entropy`** (S/k_B **1.05×10⁷⁷** @1 M☉; radius-input path), **O5 `isco`** (Schwarzschild r_ISCO **8.86 km** / 3 r_s / eff **5.72%** / v **0.5c**; Kerr BPT extremal prograde **42.3%** / retrograde 3.8%; orbital_velocity null for spin≠0; extremal-prograde 0/0 guard → 1/√3), **O6 `kerr-horizon`** (a*=0 → r₊=r_s; a*=1 → r₊=r_s/2, ergosphere=r_s, extremal flag), **O7 `bh-tidal-force`** (`2GMΔr/r³`, horizon `Δr·c⁶/4G²M²`: 10 M☉ **1.89×10⁷ g** / 1 M☉ 1.89×10⁹ / 1e8 SMBH **1.89×10⁻⁷ g**; `--threshold-g` spaghettification radius + inside_horizon for SMBH), **O8 `eddington-luminosity`** (L **1.26×10³¹ W** / 3.3×10⁴ L☉ / Ṁ **1.4×10¹⁵ kg/s** @1 M☉; Ṁ∝1/η), **O9 `unruh-temperature`** (a=2.47×10²⁰ → **1 K**, 1 g → **4.0×10⁻²⁰ K**, inverse), **O10 `bekenstein-bound`** (1 kg/0.1 m → **2.58×10⁴² bits**, human 70 kg/1 m → **1.80×10⁴⁵ bits**; energy==mc² parity). `test_black_hole.py` (in-process): anchors + the self-validating error matrix (mass/object conflict, forward-vs-inverse mutex, spin out of [−1,1], tidal distance mutex, efficiency ∉(0,1], bound radius/energy gates). `test_query_black_hole.py` is the offline subprocess contract (mirrors `test_query_active_shield.py`): happy-path + core-parity across all ten, the `--object` preset, the inverse modes, the curated exit-1 matrix, and the argparse exit-2 matrix (bad `--object` choice, non-numeric, missing required `--radius-m`). See `PHASE_AE_PLAN.md`, `docs/integration.md` (Black holes & relativistic thermodynamics).
- `test_warp.py`, `test_query_warp.py` — the **Phase AH (Group N)** Alcubierre / metric-drive calculators (`core/warp.py`), offline; `query.py`-only, Phase-N/T/V lineage (no GUI/DB/network/RNG/numpy). Two subcommands (**N1 `alcubierre-energy`** + **N2 `warp-metric`**), built in three checkpoints (AH·1 numeric core → AH·2 ladder+regime → AH·3 metric). N1 `original` is the **computed** Alcubierre-1994 T⁰⁰ integral `E=−(c²v_s²/12G)∫(df/dr)²r²dr` **joules** (`energy_kg_equiv=E/c²`) via a standalone plain-Python Simpson helper (`_alcubierre_energy_j`): the **−3.37×10⁴⁵ J** anchor (≈ −3.75×10²⁸ kg-equiv; Δ=1 m → ≈ 0.19 M☉ per Pfenning–Ford) @(R=100,v=c,Δ=10), the **∝1/Δ** and **∝v_s²** scaling pins, a **convergence pin** (helper vs an independent 4×-finer Simpson agree to <10⁻⁶) + determinism, and the resolution-cap safeguard for thin walls. The six **reduction formulations** (van-den-broeck/krasnikov/white/bobrick-martire/physical-2024/lentz) **report** literature figures/sources (a `_REDUCTIONS` data table, independently cross-checked against the request spec) with `energy_j=null`; a **regime truth-table** test pins `energy_condition_status` for every (formulation × subluminal 0.5 / superluminal 2) per the Santiago–Schuster–Visser 2021 NEC ruling (superluminal → NEC-violating-exotic always; subluminal + bobrick-martire/physical-2024 → positive-energy-possible), plus the `contested` flags (white/lentz) and the `published_figure`-present-for-reductions / null-for-original invariant. `test_warp.py` (in-process): anchors + ladder/regime + error matrix (non-positive inputs, unknown formulation). **N2 `warp-metric`** covers the tanh shape function (f(0)=1 flat interior / f(≫R)=0 flat exterior), the expansion scalar θ (antisymmetric: contraction ahead / equal expansion behind; max_expansion=−max_contraction), the 10–90% wall region symmetric about R, the `--profile` sampling (f runs 1→0), and the **Natário zero-expansion variant** (θ / max_expansion / max_contraction all 0). `test_query_warp.py` is the offline subprocess contract: happy-path + core-parity for both subcommands, a reduction-formulation run, the natário θ=0 case, the curated exit-1 matrix, and argparse exit-2 (unknown `--formulation` / bad `--variant` choice, non-numeric). See `PHASE_AE_PLAN.md` (§7a checkpoints), `docs/integration.md` (Alcubierre / metric drive).
- `test_formation.py`, `test_query_formation.py` — the **Phase AJ (Group P)** planet-formation calculators (`core/formation.py`), offline; `query.py`-only, Phase-H/P lineage (no GUI/DB/network/RNG/numpy). Six subcommands built in two stages (AJ-1 core = P1+P2; AJ-2 = P3–P6). `test_formation.py` (in-process) pins every acceptance anchor from the request spec + the frozen `formation-calculators-followup-1.md` rulings: **P1 `disk-model`** (MMSN 1 AU → Σ_gas **1700** / T **280** exact, H/r **0.0334**, Σ_solid **22.78**; 5.2 AU → **143.4**/**122.8**; own-T-law snow line **2.71 AU** at 170 K ∝ L^½, no `ice-lines` import; `--feh`/`--z`/disk-mass/`--ms-luminosity`/`--snowline-*` overrides; log-spaced grid), **P2 `isolation-mass`** (Armitage Eq. 201 → **0.066**/**9.27 M⊕**, Σ_p^{3/2}·a³·M★^{−1/2} scaling, both feeding-zone conventions), **P3 `pebble-isolation-mass`** (Bitsch 2018 → **25**/**20**(`--simple`)/**5.4**, f_fit↑ with α, H/r derivation), **P4 `gap-opening-mass`** (Crida Eq. 15 bisection → threshold_q **4.978e-4** / **0.52 M_Jup** at M⊙ via `--nu-code 3.162e-6`, criterion cross-check P(1e-3)=**0.699**, reynolds/nu-code parity, p-target monotonicity), **P5 `toomre-q`** (Armitage Eq. 164 → MMSN 30 AU **Q 23.7** stable, instability flag, c_s-mode parity), **P6 `critical-core-mass`** (Ikoma+2000 → **12**/**6.75 M⊕**, index/norm knobs) — each with its self-validating error matrix (missing/duplicate-unit/XOR-violation/non-positive gates). `test_query_formation.py` is the offline subprocess contract (mirrors `test_query_gravitation.py`): happy-path + core-parity for all six, the curated exit-1 matrix, and argparse exit-2 (non-numeric, wrong nargs, missing value, unknown subcommand). See `PHASE_AJ_PLAN.md`, `docs/integration.md` (Planet formation).
- `test_group_q.py` — the **Phase AK (Group Q)** metric-drive power/fuel + exclusion-boundary calculators (`core/metric_drive.py`, `core/exclusion_boundary.py`), offline; `query.py`-only, Phase-H/P lineage (no GUI/DB/network/RNG/numpy). In-process core tests pinning every acceptance anchor from the request spec (`metric-drive-power-and-exclusion-boundary-calculators-request.md`). **Q1 `metric-drive-power`** (field-rocket `P_rad=k·F·c`): the **0.9 GW/N** @k3 anchor, **9 PW** @1g/1000t, **4.4×10¹⁷ W** @50g, the **1 g-day leg** (Δη 2.83e-3 → f_rad **0.84%**), the fuel bill (**d-t → 2.25×** ship mass; **antimatter-pp** η_dir 1.0 → **0.84%** vs default η_dir 0.5 → **1.69%** — the crossover-sensitive pair), constant-velocity F=0 → power/f_rad 0, the beam-vs-onboard crossover (beam 0.15 / onboard 0.9 GW/N, crossover k 0.5, winner flips at k<0.5), the turn penalty (integrated arc > collinear), exact-relativistic Δv→0.9c (f_rad 0.988), the **DRY reuse assertion** (pp/dd f-values == `core.ism_drag_tables._FUSION`), and the full self-validating error matrix (k≤0 reactionless-forbidden, contradictory thrust/rapidity sources, f_conv≤0, η_dir-without-fuel, turn-without/below-|Δη|, |β|≥1, both mass units, unknown fuel). **Q2 `exclusion-boundary`** (`r_ex = DIAL·(M/M☉)^α·(L/L☉)^β·(Ẇ/Ẇ_☉)^γ`): Sun **47.5 AU**, 0.1 M☉ → **22.05/15.02 AU** (α 1/3, 1/2 via `--scan-alpha`), 10 M☉ → **102.3/150.2 AU** (`forcing_class` harbor), brown-dwarf → optional, explicit `--dial` overrides auto-calibration (and equals auto-cal at dial=47.5), the solar-wind term = 1 at the Ẇ=2×10⁻¹⁴ preset, and the error matrix (M≤0, negative exponents, non-positive dial/calibration, β≠0 with L≤0, and the **anchor-7** wind-exponent-without-wind curated error). Plus a **cross-check** that Q1's beam side agrees with `core.propulsion.compute_beam_sail`'s reflecting-sail momentum (2P/c → c/2 W per N). See `docs/integration.md` (Metric-drive power & exclusion boundary). **Phase AL (R6)** adds `MetricDriveSelfConsistent` — the `metric-drive-power-followups.md` self-consistent fuel-bill anchors: D-T 1 g-day k=3 → **infeasible / k_wall 1.329 / budget 375.4 km/s**, k=1 → **sc 3.04** (first-order stays 0.753), antimatter-pp 25 g-day k=3 → **0.529** / 50 g-day k=3 → **1.348** / k=1 → **0.329**, vent D-T k=3 → **8.59**, the small-Δη→first-order agreement (<1%), and the error/guard matrix (`--ash vent` without `--self-consistent`; `keep` mode needs a `--fuel` preset to split f/η_dir; no maneuver; the back-compat regression that no sc keys appear by default).
- `test_power.py`, `test_query_power.py`, `test_energy_storage.py`, `test_power_tables.py` — the **Phase AL (Group R)** power-generation / storage / thermal calculators (`core/power.py`, `core/energy_storage.py`, `core/power_tables.py` + R3 in `core/thermal.py`), offline; `query.py`-only, for the sibling `scifiWorldBuilding-Claude` repo's Packet 27 (**Group R** → repo Phase AL; Phase-H/P lineage, no GUI/DB/network/RNG/time/numpy). `test_power.py` (in-process): **R1 `annihilation-power-train`** (pp 1 µg/s → total 8.988e7 / directed 4.494e7 / γ 2.996e7 / ν 4.494e7; the mass-flow==power-total parity; ee no-ν; η_dir override), **R2 `antimatter-production`** (stored 1 ng / η 1e-4 → stored 8.988e7 J / in 8.988e11 J / **floor 0.3333 exact** / ratio 1e4; the Brillouin ε₀B²/2 storage density; **`production_efficiency` required — no default**; the >floor impossible-efficiency flag), **R4 `reactor-net-power`** (1 GW / η 0.4 / Q 10 → electric 4e8 / breakeven 2.5 / **net 3.6e8**; no-Q no-tax → 4e8; recirc), **R7 `beamed-power-delivery`** (λ 1 µm / D_t 10 / L 1 AU → D_spot 3.65e4 m; D_r 100 → **capture 7.5e-6**; frequency==wavelength parity; delivered power), **R10 `fusion-lawson`** (D-T triple 3e21 → **q 1 ignited boundary**; triple-from-parts; boost 3 → q 3; the scope-guard string; p-B11 ~10³× harder) — each with its validation matrix + the `model_note`-present check. `test_energy_storage.py`: **R8 `flywheel-storage`** (σ 5e9 / ρ 1800 / K 0.5 → **1.389e6 J/kg** = 386 Wh/kg; K 0.3 → 8.33e5; stored energy) + **R9 `smes-storage`** (B 20 T → **u 1.592e8 J/m³**; the structure-limited σ/ρ specific-energy branch; the `critical_field_exceeded` flag; the one-of-σ/ρ error). `test_power_tables.py`: **T1 `energy-storage`** (all-rows vs `--class li-ion`, the `--override-wh-kg` echo, the sensible `E=m·c_p·ΔT` = 4.186e8 J / latent `E=m·L` compute branches, the `f·c²` nuclear-ceiling pointer, the both-branches / unknown-class errors) + **T2 `reactor-power`** (all-rows vs `--class fusion`, the implied `core_mass_kg` P/α = 2e5 kg, the `--override-kw-kg` echo, the **mandatory `thermal_pointer` on every row**, unknown-class error). `test_query_power.py` is the offline `query.py` subprocess contract (mirrors `test_query_thermal.py`): happy-path JSON + core-parity for all 10 subcommands and the self-validating exit-code matrix (curated `{"error"}` exit 1 incl. the table unknown-class curated errors; argparse exit 2 for mutex/choice/required). R3 `heat-pump` core is in `test_thermal.py` (`HeatPumpTest`: 300→320 K lift 1 W → **COP 15 / W 0.0667 / rejected 1.0667**; the Q_c↔W round-trip; efficiency_fraction scaling; T_h≤T_c and load-anchor errors) and its subprocess contract in `test_query_power.py`; R5 fission `_FISSION` golden pins are in `test_ism_drag.py` (`FissionTableTest`). See `PHASE_AL_PLAN.md`, `docs/integration.md` (Power generation / storage / thermal).
- `test_query_exposure_additions.py` — two `query.py` subcommands added after a coverage audit to close exposure gaps, each a thin verbatim wrapper over a **non-self-validating** legacy core function (so they share the Phase-N contract: happy-path exit 0, raw-exception `{"error"}` exit 1 for an out-of-range numeric, argparse exit 2): `distance-at-acceleration` (`calculators.compute_distance_at_acceleration` — opt 24, accel + time → 3-profile distances; the inverse of `brachistochrone-au/-lm`) and `star-regions-manual` (`regions.compute_star_system_regions` — opt 10, manual vmag/BC/teff/parallax → full region values, no SIMBAD). Offline subprocess happy-path + core-parity + the exit-code matrix (zero-accel / zero-or-negative-parallax raw exceptions; defaults + override of `--sunlight-intensity`/`--bond-albedo`; argparse missing/non-numeric exit 2). Same subprocess harness as `test_query_phase_n.py`. **Also** the six **Group-A velocity/distance/time converters** (opts 25–28/31/32 — `ly-hr-to-times-c`, `times-c-to-ly-hr`, `distance-traveled-ly-hr`/`-times-c`, `travel-time-ly-hr`/`-times-c`; another audit-driven exposure-gap close), each a thin wrapper over a non-self-validating `core.calculators` converter: happy-path + core-parity, the travel-time pair's zero-velocity division-by-zero exit 1, and argparse exit 2 for missing/non-numeric args.
- `test_comparison.py` — the Phase L1–L3 (Comparison Dashboard) core + integration contracts, offline. `compute_stellar_evolution` (L3): the 1 M☉ MS anchor, contiguous/non-overlapping stage boundaries summing to `total_gyr`, `current_stage` selection (in-MS / Beyond AGB), the low-mass (< 0.8, only Pre-MS+MS) and high-mass (> 8, "Supergiant → Supernova") branches, and the self-validating out-of-range errors. `compare_stars` (L1, network mocked — SIMBAD/Hypatia/`_query_tap`): per-star error isolation, the NASA pscomppars supplement + `radius²·(teff/5778)⁴` luminosity + HZ-bound path, the carried-through Hypatia sub-dict, and the **Sol/Sun reference-constant special-case** (SIMBAD-bypassing injected Sun values + the `[X/H]≡0`, `n=0` solar baseline that `prepare_abundance_comparison` fills-but-doesn't-bloat). The `core.viz.prepare_evolution_diagram` / `prepare_abundance_comparison` shapes. The `stellar-evolution` query.py subcommand happy-path + parity + exit-code matrix (curated-error exit-1 since it self-validates; argparse exit-2). L2 (ESI ranking) adds no core fn — it reuses `search_hwc` (covered by `test_query_expanded.py`); its L2 bar-chart prep `core.viz.prepare_esi_bar_chart` (ESI filter/sort/top-N cap, habitable flag, error passthrough) is covered here too. The L1 `compare-stars` query.py subcommand is covered by a subprocess contract (offline happy path via the Sol/Sun reference-constant special-case; arg-count exit-1 for < 2 / > 4; argparse exit-2).
- `test_hypatia_cache.py` — the Phase L4 (Hypatia abundance cache + search) core + integration contracts, offline. `import_hypatia_cache` with the bulk `GET /data` fetch (`_hypatia_data_fetch`) mocked: row assembly (props + denormalized `fe_h` + precomputed `light_years` + disk int-formatting), idempotency / no-orphans, a non-fatal element-axis failure (`errors` count), and the validate-before-destroy **Gate 1** (a short download leaves the cache intact). `search_hypatia_cache`: the filter matrix (`fe_h`/`teff`/`ly_max`/`disk`/`element`-EXISTS), `fe_h DESC` NULL-last ordering, the pivoted `mg_h`/`si_h`/`o_h`, and the empty-table error. The `search_star_systems` **`fe_h` JOIN** (activates only with a populated `hypatia_cache`; matches nothing — not an error — when empty; the non-fe_h path is unaffected by the `ss`-alias rewrite). The `search-hypatia` query.py subcommand (subprocess happy-path + no-filter + argparse exit-2). The L4 scatter-diagram prep `core.viz.prepare_hypatia_scatter` (rows dropped when either axis is non-numeric; bad-axis/empty errors). Same DB-isolation pattern as `test_db_backups.py` (in-process tmp DB) + `SPACE_APP_DB` subprocess.
- `test_hz_strip.py` — Phase 5 (HZ Rings/Strip toggle); offline + offscreen. `PrepareHzStripTests` — pure `core.viz.prepare_hz_strip`: the Sol band edges vs canonical Kopparapu (opt 0.75–1.77 / con 0.95–1.68), the per-planet `in_hz` flags (Earth/Mars in, Venus/Mercury out), bands-only when no planets, bad/missing-SMA planets dropped, `max_au` expands for an outer planet, error passthrough. Qt/matplotlib-gated: `HzStripCanvasTests` — `make_hz_canvas` still builds (Rings unchanged), `make_hz_strip_canvas` builds with planets / bands-only / `(None, None)` without bands; `HzToggleWrapperTests` — `_hz_toggle_tab` yields a 2-view `QStackedWidget` with Rings default (index 0) and the Strip button switching to 1, a bands-only tab still gets the toggle, and `wrap_hz_with_toggle` falls back to the bare rings widget when the strip factory returns None (and returns None when rings can't build).
- `test_viz_phase_o.py` — the Phase O (visualization expansion) test scaffold; offline + offscreen (`QT_QPA_PLATFORM=offscreen`). **Sub-phase O-1 (Shared Foundations)**: **F1** — the additive `app_magnitude`/`parsecs` keys on the opts-18/19 result rows (`compute_stars_within_distance_of_sol`/`_of_star`; tmp `star_systems` DB, SIMBAD-mocked centre, asserts the new keys *and* that pre-existing keys are untouched); **F2** — the reusable help-dialog component (`gui/help.py` `show_help_dialog`/`info_button` + `gui/help_text.py` `TOOMRE_HELP_HTML`) via an offscreen `QApplication` smoke; **F3** — the shared `build_canvas_ok` offscreen helper + the additivity-regression guard that builds every shared canvas (`make_star_chart_canvas`/`_3d`, `make_star_map_canvas`/`_3d`, `make_orbits_canvas`) with no new kwargs (guards opts 18/19 / GCNS / Phase-I callers against future signature breaks). **Sub-phase O-2 (Star-Map Data Products)**: `core.viz.prepare_sky_from_star` (O1 — vantage RA/Dec + distance-modulus magnitude, `skipped_no_mag`, mag-limit filter, Sol appended), `prepare_hr_main_sequence` (O2a — tmp DB w/ seeded `main_sequence_stars`, hot→cool sort, empty-table error) and `prepare_hr_from_stars` (O2b — `M_V` + ceiling-rule Teff, skip counts; resets `core.regions._MAIN_SEQUENCE_DATA`); `make_hr_canvas`/`make_sky_canvas` offscreen smokes; and a panel-wiring smoke that constructs `MainSequencePanel`/`StarsWithinDistance{Sol,Star}Panel` and exercises the `_add_hr_tab`/`_add_night_sky_tab` builders. Later O-sub-phases append their `prepare_*` unit tests here. **O-5 (O8 two-star map, opts 17/20/21)** covers the **Sol-centered** rebuild of `route_planning._two_star_route_map`: Sol is `stars[0]` at the origin (an endpoint that *is* Sol becomes the centre rather than a duplicate), the searched stars keep their true heliocentric coordinates and take spectral-class colours from the additive `sp_type` (absent → the grey fallback, which is what stubbed lookups produce), and `edges` is always empty; plus canvas smokes and panel wiring — both tabs built by the opt-18/19 `_build_iso_chart_tab` (asserted via the O17 isochrone control being present) and the optional `link_view` O15 row↔map wiring, and the additive `label_max_ly` Star-Chart kwarg (raised threshold → labels visible at the two-star charts' range; default `None` → the shared 15 ly cutoff still hides them, which guards the opts-18/19 / GCNS / Phase-I callers). See `PHASE_O_PLAN.md`.

- `test_report.py`, `test_reports_panel.py` — the Phase Q **System Dossier** (`core/report.py` + `DossierExportPanel`), offline (mocked readers; the Sol path runs offline against an auto-seeded throwaway `SPACE_APP_DB`). `test_report.py` covers `build_system_dossier`: the normal-star path (identity / regions + full Phase P solvent+ice surface / Kopparapu HZ / planets — **both** NASA pscomppars [priority 1] + HWC [priority 2] sub-tables / Hypatia all-species grouped by family / the M5 GCNS block), the **markdown / html / json** renderers (json = structured `data` only; html self-contained text+tables, **no `<img>`**), the three-tier validation (hard `{"error"}` for SIMBAD-fail / bad fmt / bad section; `warnings[]` for a per-source failure or a requested-but-unavailable section; `notes[]` for by-design omissions), determinism, the **Sol/Sun reference-origin path** (offline — no SIMBAD; `compute_sol_regions` + real `compute_solar_system_tables` planets/dwarfs/asteroids + the `_sun_hypatia_baseline` zero-point + GCNS-N/A note; `moons` opt-in), and the `query.py dossier` subprocess contract (happy-path / json-shape / section subset / argparse exit-2 / unknown-section exit-1). `test_reports_panel.py` is the headless GUI smoke (offscreen): panel construction, default/`moons` section selection, the markdown/html render path (option-A inline base64 figures spliced into HTML when matplotlib is present), error/warning status surfacing, and the nav/export registration.
- `test_generate.py`, `test_query_generate.py`, `test_generator_panel.py` — the Phase R1 **procedural system generator** (`core/generate.py` + `core/priors.py` + `SystemGeneratorPanel`), offline. `test_generate.py` (the core suite): `DefaultPriors` (the R3 seam) + the two new astronomy helpers `_classify_planet` (G3 — mass-class boundaries + snow-line modifier + per-type radius) and `_equilibrium_temp` (G4 — the Phase P `implied_edge_temp` equilibrium wrapper); the **synthetic mode** of `generate_system` (the headline **determinism** contract — same seed → deep-equal output; star Teff/M/R/L consistent with the main-sequence interpolation + `compute_star_luminosity`; HZ flags from `compute_habitable_zone`; `n_planets` honoured; classifier boundaries; moons strictly between `compute_roche_limit` and `compute_hill_sphere`; `t_eq_k` falls with `a_au`; `require_habitable` delivers a conservative-HZ rocky world or errors after bounded retries; bad inputs → `{"error"}`); and the **real-anchor mode** with `compute_simbad_lookup` / `compute_star_system_regions_from_simbad` / `compute_planetary_systems_composite` / `compute_hwc` **mocked** (observed planets flagged `source:"observed"`, synthetic extensions `source:"synthetic"`, synthetic SMAs never colliding with observed, multiplicity via a mocked GCNS `n_components=2` → warning + safe-cap, a white-dwarf-regions anchor → error). `test_query_generate.py` is the offline `query.py generate-system` subprocess contract (synthetic happy-path JSON + anchor values, cross-process seed determinism, the curated-error exit-1 matrix — self-validating, unlike the Phase-N raw-exception wrappers — and argparse exit-2 for missing/non-integer args; harness mirrors `test_query_phase_n.py` with a throwaway `SPACE_APP_DB`). `test_generator_panel.py` is the headless GUI smoke (offscreen): construction/mode-enable (anchor field vs spectral chips), single-select chips + class building, a synthetic generate building the Planet Table + Orbit Diagram / HZ Ring viz tabs, the Auto (sampled) planet count, bad-seed + error-result paths, and nav/export registration. (Phase R2 extends `test_query_generate.py` with the `--constraint` DSL / feasibility contract and `test_generator_panel.py` with the constraint-builder + four-layer cards — see the next bullet.)
- `test_feasibility.py`, `test_nbody.py` — the Phase R2 **constraint / feasibility engine** (`core/feasibility.py` + `core/nbody.py`), offline. `test_feasibility.py`: the new-physics helpers **G1** (`mutual_hill` — the Gladman 2√3 Hill floor + Chambers Δ≥10 long-term threshold; anchored to Solar-System pairs incl. Jupiter–Saturn in the gray band) and **G2** (`period_ratio`/`nearest_mmr`/`in_mmr` + the Gascheau/Routh `gascheau_coorbital_stable` co-orbital criterion; Jupiter-Trojan, Neptune/Pluto 3:2, 2:1); the **rule registry + `evaluate_feasibility`** for the core-4 vocab (`planet_at_location`/`trojan`/`moon`/`resonance`) + the stretch vocab (`habitable_world`/`alt_solvent_world`/`architecture`) — verdict + the four layers, the Δ-band boundaries, `_resolve_ref`/`_resolve_location`, the **determinism** contract, unknown-type/unresolvable-ref → `not_evaluated`, base-error passthrough, and **0-constraint parity** (`generate_system` with no constraints = the R1 path, byte-identical); **Layer-3** tagged origin (`grounding=default-extrapolation`); **Layer-4** deterministic alternatives whose `spec_patch` re-runs to feasible; **R2-C4** `_nbody_confirm` upgrading/downgrading a marginal packing verdict under `nbody=True`; **R2-C5** the multi-star S/P-type gate (`_binary_gate`/`_apply_binary_gate` over `compute_binary_orbit_stability` + a spec `companion` hint, the no-hint real-anchor-multiple note via mocked `core.generate.*` readers). `test_nbody.py`: the pure-numpy leapfrog `integrate_coplanar` — determinism (no RNG), a widely-separated system surviving the bounded run, packed giants flagged unstable (close encounter), and the trivial/bad-input short-circuits. Phase R2 also extends `test_query_generate.py` (the `generate-system --constraint` DSL parse + feasibility envelope contract, companion gate, unknown→not_evaluated, malformed-constraint/companion/bad-ecc exit-1, 0-constraint parity) and `test_generator_panel.py` (the constraint-builder rows + `to_spec`, feasibility cards, clickable-apply alternatives, the depth cap, N-body checkbox).
- `test_research_priors.py` — the Phase R3 **research-priors hook** (`core/research_priors.py` + `core/priors.py`'s `ResearchPriors`/`get_priors`), offline. The **data contract** validator (`validate_priors_contract` — both committed fixtures validate; every malformed axis/metadata/`origin_priors` shape → curated `{"error"}`; the identity fixture's sampling axes are exact `DefaultPriors` clones); **`ResearchPriors`** provider parity (same attribute surface + `origin_priors`/`version`, `n_planet_dist` keys coerced to int) + the **`get_priors`** selector (permissive→`DefaultPriors`, strict+cache→`ResearchPriors`, strict+no-cache→`PriorsUnavailable`, bad→`ValueError`); the **importer** `compute_research_priors_ingest` (validate-before-store Gate-1 — a bad contract writes nothing / preserves an existing cache) + `get_research_priors_status`. The two committed fixtures live in `tests/fixtures/` (`research_priors_sample.json` perturbed, `research_priors_identity.json` a DefaultPriors clone); the live cache is the gitignored `data/research_priors/` (override via `SPACE_RESEARCH_PRIORS_DIR`, mirroring `SPACE_APP_DB`). **R3 also extends:** `test_generate.py` (the `research_policy` seam through generation — **permissive byte-identical**, strict+identity == permissive-except-the-grounding-badge, strict+perturbed deterministic-and-different, strict+no-cache → curated error, real-anchor note re-tag), `test_feasibility.py` (Layer-3 reads `origin_priors` with per-key heuristic fallback tagged `default-extrapolation` even under strict; `evaluate_feasibility` strict calibrated+deterministic / no-cache error), `test_query_generate.py` (the `generate-system --research-policy` subprocess contract — permissive parity, strict-with-cache via `SPACE_RESEARCH_PRIORS_DIR`, strict-no-cache exit 1, bad-value exit 2), and `test_generator_panel.py` (the research-policy selector + grounding-badge flip; the `ImportResearchPriorsPanel` smoke).
- **R3-V2 B1 (stellar multiplicity)** extends `test_generate.py` with two classes, offline. `TestStellarMultiplicity` locks `_draw_multiplicity` against the block's own targets: the mass-dependent multiplicity fraction (linear in log₁₀ M, endpoints held) reproduces `multiplicity_fraction` at 0.3/1.0 M☉; the close-pair share of multiples lands on the **0.087 by-construction** mixture weight; the wide component stays **disjoint** from the close-pair window (F-2 truncation — no wide draw below 62.6 d); `q` stays inside `[q_min, q_max]` and `m₂ = q·M₁`; `sma_au`/`p_orb_days` round-trip through Kepler III. Two **contractual behaviours** get their own tests because each encodes a defect that would otherwise be silent: eccentricity is **never identically zero** (20k draws), and the circularization period is a **statistical boundary, not a cut** — an eccentric draw must be possible inside it, or BY Dra (`e = 0.300` at `P = 5.98 d`) would be unreproducible. Plus the shape/provenance guarantees: the emitted companion matches the dataset's `consumer_contract.emit` set exactly, higher-order systems are counted **and disclosed** as not-placed, the note names the `f(e)` shape as an app-side choice, the **absent block consumes no rng** (so a v1 dataset stays byte-identical) and `DefaultPriors` carries no block, the v2 provenance note claims the block only in **synthetic** mode (never under a real anchor, whose multiplicity is GCNS-derived), determinism holds, and planets are emitted in **orbital order**. `TestDrawnCompanionReachesTheBinaryGate` covers the feasibility seam: a drawn companion drives the existing S/P-type gate when no hint is supplied, and an explicit `--companion` always outranks it.
- **R3-V2 B2 (age + activity)** adds `TestAgeAndActivity` to `test_generate.py`, offline. `age_dist`: the block absent → `None` with **no rng consumed**; **no star older than its own main sequence** across four masses (the block's `truncate_and_renormalize` acceptance, via the Phase-L3 `compute_stellar_evolution`); massive stars draw younger than low-mass ones (directional — the block's own `mass_conditional_age` table, not a point match); the low-mass mean tracks that table within 0.8 Gyr; the **BGM discrete-age-bin artifact is smoothed, not reproduced** (the zeroed 7–8 Gyr bin must still receive draws) — and smoothing is **skipped** when the dataset ships no `sfh_smoothing_note`, so we never quietly reshape a histogram the dataset did not flag. The validator's two structural guards get negative tests: a non-contiguous bin → error (a gap silently drops probability mass), and an interior zero-fraction bin **without** a smoothing note → error (the artifact cannot arrive undocumented). `stellar_activity`: the dataset's **own `unit_test_sun`** is the anchor (M=1 at 4.57 Gyr → P_rot 25.4 d, τ 13.8 d, Ro 1.84, log R_X −5.99, inside the observed solar cycle band, no domain flags) plus a drift guard asserting the shipped τ **formula string** still matches the hardcoded implementation; the **locked branch runs with no age at all** and is saturated; the locked-vs-single X-ray contrast lands inside the block's `orders_of_magnitude` acceptance band as an **emergent** value (`is_prior_field: false` — it must never be fed in); the **XUV contrast is softer than the X-ray contrast** and the saturated star is *less* EUV-dominated than the quiet one (the block's headline physical claim, which runs opposite to intuition); the contested X-ray→EUV conversion **names its relation and lists the alternative** rather than averaging them; `circumbinary_component_scaling` is 1.0; M-dwarf singles are bimodal and **never draw inside the excluded gap**; out-of-domain Rossby numbers are **flagged but still reported**. Wiring: a synthetic star carries `age_gyr`/`age_source`/`activity` and names both blocks in the v2 note, while **anchor mode claims neither**.
- **R3-V2 B3 (wide-companion outer bound)** adds `TestWideCompanionDisruptionBound` to `test_generate.py`, offline. `a_max = 1.212 × (M_tot/t)` pc reproduces the source's worked examples (1 M☉ → 1.212 pc at 1 Gyr, 0.121 pc at 10 Gyr; 0.25 M☉ → 0.030 pc at 10 Gyr); the bound **moves** with mass and age (it is not a constant, which is the whole point); **no age → no bound**, because a constant stand-in would be exactly the fixed cutoff the sources deny; no wide companion survives beyond its own limit; the emitted note **labels the scale a HALF-LIFE and the truncation a modelling convenience**, and asserts the source's "no breaks or cutoffs" finding travels with it — the secondary source's "widest surviving binary" gloss is exactly the misreading the test exists to prevent, and the retracted `wide_disruption_coeff_slack` field is asserted **absent** so it cannot return; the note records **why no tail is added** (the join normalization is *unknown*); the **solar-host shape caveat is disclosed** on solar hosts and absent on M hosts (which err safe); and close pairs are unaffected by the bound.
- `test_projects.py`, `test_query_projects.py`, `test_projects_panel.py` — the Phase S **Project Workspaces** (campaign/novel manager), offline. `test_projects.py` (the `core/projects.py` CRUD over the additive `projects`/`project_members` tables; `core.db._DB_PATH` tmp-swap): create/list/get/add/update-note/remove/rename/delete round-trips, blank/duplicate-name + unknown-project errors, idempotent re-add, the **D2 collision suffix** (a distinct same-named generated member → `" (2)"`) vs same-seed idempotency, `delete_project` cascade, `get_table_status` rows, and the **headline generated-spec round-trip** (`add_member(spec=…)` → `get_project` returns the parsed spec → `generate_system(**spec)` deep-equal to the original — the R determinism contract in the workspace; that class leaves auto-seed on). `test_query_projects.py` is the offline `query.py project-list` / `project-get` subprocess contract (read-only; member counts, parsed-spec echo, unknown-name exit 1, argparse exit 2; per-test tmp `SPACE_APP_DB` seeded in-process). `test_projects_panel.py` is the headless GUI smoke (offscreen): `ProjectPanel` master-detail (create/select/members/inline-note/remove, Open generated re-creates / Open real dispatches without network), the **Export Project Dossier** dialog + `_write_export` (combined file / json / per-file), `choose_and_add` (existing/new/cancel), the **"Add to project"** entry points on `SimbadPanel` + `SystemGeneratorPanel`, and the DbStatus rows. **Phase S also extends** `test_report.py` (`build_generated_dossier` md/html/json + the `build_project_dossier` fan-out — combined/per-file/json, per-member failure isolation, unknown-project/bad-fmt; mocked `build_system_dossier` + `generate_from_spec`, fully offline).
- `test_binary.py`, `test_catalog_cache.py` — the Phase AM **catalog-access tier** offline foundation (pure math + filesystem, **always run**, no network). `test_binary.py` locks the load-bearing §3.3 **companion-mass classifier** in `core/binary.py`: the cubic mass-function solver (forward-then-solve round-trip), the Thiele-Innes → a₀ path with the closed-form `a₀ = max(|A|,|G|)` anchor when B=F=0 (→ m₂ ≈ 0.5 M☉ for the constructed orbit), the SB1 `f(m)` formula, the star/BD/planet thresholds (0.075 / 0.013 boundaries) + the a₀ ≲ 1 mas `low_significance` flag, `m1_from_spectral_type` (Pecaut-Mamajek ladder; unknown/degenerate → 1.0 default), and the verification-tag formats. It also covers the **census dedup** (`TestCensusDedup`, offline, hand-built rows): intra-source collapse to the highest-graded NSS solution with the discarded ones **surfaced** (`n_orbit_solutions`/`other_solutions`), identity-first cross-route single-counting, and `period_disagreement` raised when two routes describe one star with periods >5% apart. Four **real** systems pin four different rules, and the two that must NOT collapse matter as much as the two that must: **FK Aqr** and **BY Dra** (a low-grade `OrbitalTargetedSearch` against an SB2/SB2C matching the primary paper) both resolve *mechanically* by grade with no per-system casing; **G 184-19** (sole `SB2`, grade 126) and **Wolf 227** (sole `OrbitalTargetedSearch`, grade 38) both survive with a `sole_solution` flag and **no manufactured verdict** — the test asserts they are distinguished by type + rival-row existence, not by grade; and **Castor** (two genuine close pairs at one position) must stay two rows, covered both via the positional path (which flags, never merges) and via the identity path (a second SB9 orbit on one Gaia source is kept and flagged). Plus the `dedup` accounting block. It also covers the §3.3 **Gaia `binary_masses` cross-check** (`_apply_binary_masses`: none-passthrough, fill-when-our-method-absent-and-Gaia-m2-present, no-fill-when-Gaia-m2-null, and cross-check-with-`agreement_pct`-without-mutating), and the **`close-binary-census --include` honesty** (unknown token → curated error; a requested-but-unimplemented `wds`/`cv` lands in `coverage.requested_not_implemented`, **never** silently dropped — mocked `_census_nss`/`_census_sb9`, no network). `test_catalog_cache.py` locks `core/catalog_cache.py` (cache dir monkeypatched to a tmp path): stable order-independent hashing, TTL expiry, **never-cache-errors/empties**, miss-graceful reads, the `cached()` producer-once wrapper, and the `SPACE_APP_CATALOG_CACHE=0` disable switch.
- `test_besancon.py` — the Phase AM `besancon-query` (`core/besancon.py`) offline coverage (**no network**, matching astroquery's own choice to disable all live Besançon testing). Covers `_parse_besancon_output` on a **real captured `m1612` header+row fixture** (49 columns; asserts Age/Mass/Pop/[M/H]/Teff/Dist map correctly), the `_pop_group` thin/thick/halo/bulge mapping, `build_age_dist` on hand-built rows with known values (histogram, mean/median age, population mix, AMR, feh mean), and the validation/credential gates (missing direction, `--area` cap, bad distance, and the missing-`BESANCON_USER`/`BESANCON_PASS` → curated error — all returning **before any network**). A live end-to-end check is manual + opt-in, never in CI.
- `test_catalog_live.py` — the Phase AM **live-network** anchors, gated on CDS / ESA / HEASARC reachability (`tests/_netcheck.cds_reachable` / `esa_gaia_reachable` / `reachable`), skipped cleanly offline. Every expected value was fetched live 2026-07-23; assertions are tolerant (catalogue drift on counts, precision on periods/masses). Covers the spec §6.2 IDs: **T6** VizieR `B/cb/cbdata` GK Per Orb.Per=1.996803 + the SB9 truncation flag + bad-catalog error shape; **T8** X-Match Capella→HIP 24608 Plx 76.2; a Gaia sync/structured by-id pull; **T1** δ Tri SB9 10.020 d gr4 stellar; **T2** the GJ 876 planet-filter regression (61.36 d → class `planet`); **T3** HD 110833 method-correctness (a₀→mass ≈ 0.16 M☉ vs Gaia binary_masses 0.171); **T4** Capella SB9 104.02 d gr5 / 42.8 ly (Gaia NSS saturates); the WDS epoch-dedup invariant; **T5** the `close-binary-census` 65 ly / 365 d sweep (tolerant counts + GJ 876 in `excluded_planets`) + input validation; **T7** `gaia-astrophysical` η Cas A FLAME age 10.06 [8.90–11.19] Gyr; and a HEASARC cone smoke. `SPACE_APP_CATALOG_CACHE=0` forces the live path.
- The `*_live.py` files (`test_gcns_live.py`, `test_hypatia_live.py`, `test_catalog_live.py`) hit the **live network** and are gated by `tests/_netcheck.py` via `@unittest.skipUnless(...)` (skipped automatically when GAVO/Hypatia/CDS/ESA/HEASARC is unreachable). Tests that touch the SQLite store never mutate `data/space_app.db`: in-process tests monkeypatch `core.db._DB_PATH` to a tmp file with auto-seeding disabled (pattern in `tests/test_gcns.py`, `tests/test_regions.py`, `tests/test_db_backups.py`), and the `query.py` subprocess tests pass a throwaway DB via the `SPACE_APP_DB` environment variable.
