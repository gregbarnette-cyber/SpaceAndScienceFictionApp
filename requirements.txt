astroquery
astropy
requests
PySide6
matplotlib>=3.6,<4
numpy
pyvo

# Test-only (the suite is unittest.TestCase-based; the stdlib runner needs no extra
# deps, but pytest runs the same suite unchanged — see CLAUDE.md "### Tests").
pytest
