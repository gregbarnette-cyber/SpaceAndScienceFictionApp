astroquery
astropy
requests
PySide6
matplotlib>=3.6,<4
numpy
pyvo

# Test-only — pytest is the test runner (the app itself does not need it). The tests
# are written as unittest.TestCase classes, which pytest collects natively.
# See CLAUDE.md "### Tests".
pytest
